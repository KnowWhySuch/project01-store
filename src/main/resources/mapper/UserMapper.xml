<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.cy.store.mapper.UserMapper">

    <!--
        useGeneratedKeys:开启某个字段的值递增（主键设置为递增）
    -->
    <insert id="insert" parameterType="user">
        insert into t_user
        values
        (null ,#{username},#{password},#{salt},#{phone},#{email},#{gender},#{avatar},#{isDelete},
         #{createdUser},#{createdTime},#{modifiedUser},#{modifiedTime})
    </insert>

    <!--
        resultType:表示查询的结果集类型，只需要指定对应映射类的类型。
        resultMap：表示当表的属性和类的对象属性的字段名称不一致时，自定义查询结果集的映射规则
    -->
    <!--
    自定义映射规则:
        id属性：给这个规则起名字
        type属性：取值是一个类，表示的是数据库中查询结果与java中哪个实体类进行结果集的映射
    -->
    <resultMap id="UserEntityMap" type="user">
        <!--
            将表的资源和类的属性不一致的字段进行匹配指定，名称一样的字段可以不写。主键不能省略
            column属性：表示表中的字段名
            property属性：表示类中的属性名称
        -->
        <result column="uid" property="uid"></result>
        <result column="is_delete" property="isDelete"></result>
        <result column="create_user" property="createdUser"></result>
        <result column="create_time" property="createdTime"></result>
        <result column="modified_ser" property="modifiedUser"></result>
        <result column="modified_time" property="modifiedTime"></result>

    </resultMap>
    <select id="findByUsername" resultMap="UserEntityMap">
        select * from t_user where username = #{username}
    </select>
</mapper>